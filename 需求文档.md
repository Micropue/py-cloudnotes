# 云笔记系统 — 需求规格说明书

> **版本**: v1.0  
> **日期**: 2026-06-12  
> **作者**: 单人项目  
> **开发周期**: 3天（AI辅助开发）  

---

## 1. 项目背景

### 1.1 为什么要做？
个人学习/练手项目，旨在通过 AI Agent 辅助，快速构建一个具备**实时协作能力**的 Markdown 云笔记系统。

### 1.2 解决的问题
- 提供 Markdown 文档的在线编辑与管理
- 支持多人**实时协作**同一篇文档（Google Docs 级别：光标可见、实时同步）
- 提供简洁大气的用户界面

### 1.3 参考竞品
无特定竞品对照，追求"能用、好看、核心功能完整"。

---

## 2. 项目目标

### 2.1 核心目标
3天内完成一个**可演示、可运行**的云笔记系统，包含：

1. 用户注册/登录
2. 笔记 CRUD（Markdown 格式）
3. **多人实时协作编辑**（光标同步、实时字符变更）
4. 文档评论
5. 简单大气的 UI

### 2.2 成功标准
- 所有 P0 功能完整可用
- P1 功能视进度争取完成
- 后端能通过 Docker 一键启动
- 前端 `npm run dev` 即可运行
- 界面简洁大气

---

## 3. 目标用户

| 维度 | 说明 |
|------|------|
| **用户范围** | 个人使用（可多人注册账号，属于功能演示范围） |
| **技术水平** | 非技术人员为主（产品、运营、写作），因此需要分屏预览降低 Markdown 门槛 |
| **并发量** | ≤5 人同时在线，同一文档 ≤5 人同时编辑 |
| **设备** | 桌面端浏览器（暂不考虑移动端） |

---

## 4. 用户场景

### 场景 A：个人写作
1. 用户注册/登录
2. 创建一篇新笔记
3. 在左侧编辑 Markdown，右侧实时预览
4. 保存后笔记出现在列表中

### 场景 B：多人协作
1. 用户 A 创建文档并分享给用户 B
2. 两人同时打开文档
3. 双方能看到彼此的**光标位置**和**实时字符变更**
4. 在评论区交流

### 场景 C：文档管理
1. 用户查看笔记列表（平铺展示）
2. 可搜索笔记标题（P1 阶段用 MySQL LIKE）
3. 可删除不需要的笔记

---

## 5. 功能需求（MoSCoW 优先级）

### 5.1 P0 — 必须完成（3天内）

| 编号 | 功能 | 描述 |
|------|------|------|
| F01 | **用户注册** | 用户名 + 密码注册 |
| F02 | **用户登录** | JWT Token 登录，Token 过期后重新登录 |
| F03 | **创建笔记** | 新建空白 Markdown 笔记 |
| F04 | **编辑笔记** | Markdown 编辑器（左侧编辑，右侧实时预览） |
| F05 | **删除笔记** | 删除笔记（软删除或确认后硬删除） |
| F06 | **笔记列表** | 平铺展示用户有权限的所有笔记 |
| F07 | **分屏预览** | 左编辑 / 右预览，预览实时更新 |
| F08 | **实时协作 — 内容同步** | 多人同时编辑，字符变更实时广播给所有在线用户 |
| F09 | **实时协作 — 光标同步** | 看到其他协作者的**光标位置**和**用户名** |
| F10 | **在线状态显示** | 显示当前文档有哪些人在线编辑（头像/用户名列表） |
| F11 | **登录鉴权** | 未登录用户只能看到登录/注册页 |

### 5.2 P1 — 应该完成（时间允许）

| 编号 | 功能 | 描述 |
|------|------|------|
| F12 | **文档评论** | 每篇文档底部有评论区，可发表/查看评论 |
| F13 | **文件上传** | 支持上传图片（≤4MB），在 Markdown 中引用 |
| F14 | **文档分享** | 生成分享链接，设置可读/可写权限 |
| F15 | **团队空间** | 创建团队空间，邀请成员加入 |
| F16 | **笔记重命名** | 笔记标题可修改 |

### 5.3 P2 — 可以延后

| 编号 | 功能 | 描述 |
|------|------|------|
| F17 | **版本历史** | 保留最近10个版本，7天内可回滚 |
| F18 | **全文搜索** | MySQL LIKE 搜索笔记标题和内容 |

### 5.4 P3 — 明确不做

- 笔记导出（PDF/HTML）
- 移动端适配
- 离线编辑
- 国际化
- 开放 API
- 富文本编辑

---

## 6. 非功能需求

### 6.1 性能
- 页面首屏加载 ≤2 秒
- WebSocket 消息延迟 ≤200ms（局域网内）
- 支持同时在线 ≤5 人，同一文档 ≤5 人编辑

### 6.2 可用性
- 界面简洁大气（符合"作业展示"标准）
- Markdown 语法提示/工具栏（可选，降低非技术人员使用门槛）

### 6.3 安全性
- 密码哈希存储（bcrypt）
- JWT Token 鉴权
- API 权限校验（用户只能操作自己的文档或被分享的文档）

### 6.4 可维护性
- 后端 Docker 一键部署
- 前端 `npm run dev` 即可开发
- 前后端分离

---

## 7. 技术栈

| 层 | 技术 | 说明 |
|----|------|------|
| **前端框架** | Vue 3 (Composition API) | 最新版本 |
| **UI 组件库** | PrimeVue | 轻量、现代、Vue 3 原生支持 |
| **Markdown 编辑器** | CodeMirror 6 或 Monaco Editor | 支持语法高亮、光标位置获取 |
| **Markdown 渲染** | markdown-it | 客户端渲染预览 |
| **实时协作** | Yjs (CRDT) | 在前端处理冲突合并 |
| **WebSocket 通信** | y-websocket 或自定义 WebSocket | 后端仅做消息中继 |
| **HTTP 请求** | Axios | REST API 调用 |
| **后端框架** | Python FastAPI | 异步高性能 |
| **WebSocket** | FastAPI WebSocket | 原生支持 |
| **数据库** | MySQL | 用户熟悉的技术栈 |
| **ORM** | SQLAlchemy (async) | 异步数据库操作 |
| **用户认证** | JWT (python-jose) | Token 鉴权 |
| **密码加密** | bcrypt (passlib) | 安全存储 |
| **文件存储** | 本地文件系统 | 简单场景，不引入对象存储 |
| **部署** | Docker + Docker Compose | 后端容器化 |
| **前端运行** | Vite + npm run dev | 开发模式 |

### 为什么选 Yjs（CRDT）+ 简单后端中继？

| 方案 | 复杂度 | 适合本项目的原因 |
|------|--------|------------------|
| **OT（Operational Transformation）** | 高 | 需要服务端维护文档状态，逻辑复杂 |
| **Yjs CRDT** ✅ | 中低 | 冲突解决在客户端完成，后端只需**转发消息 + 持久化**，无需 Redis，MySQL 直接存储 |
| **简单锁机制** | 低 | 不支持真正的实时协作 |

**结论**：Yjs 在前端处理 CRDT，FastAPI WebSocket 仅做消息广播 + 文档落库，架构最简、最合适。

---

## 8. 架构概要

```
┌─────────────────────────────────────────────┐
│  前端 (Vue 3 + PrimeVue + Yjs)              │
│  ┌──────────┐  ┌──────────┐  ┌───────────┐  │
│  │ 登录/注册 │  │ 笔记列表  │  │ 编辑器组件 │  │
│  └──────────┘  └──────────┘  │ - 编辑区   │  │
│                              │ - 预览区   │  │
│                              │ - 评论     │  │
│                              │ - 协作者   │  │
│                              └───────────┘  │
│  ┌──────────────────────────────────────┐    │
│  │ Yjs 文档 (CRDT) ←→ WebSocket        │    │
│  └──────────────────────────────────────┘    │
└──────────────────┬──────────────────────────┘
                   │ HTTP REST + WebSocket
┌──────────────────▼──────────────────────────┐
│  后端 (FastAPI + Docker)                    │
│  ┌──────────┐ ┌──────────┐ ┌────────────┐  │
│  │ Auth API │ │ Note API │ │ WS Handler │  │
│  │ 注册/登录 │ │ CRUD     │ │ 消息广播   │  │
│  │ JWT签发  │ │          │ │ 状态管理   │  │
│  └──────────┘ └──────────┘ └────────────┘  │
│  ┌──────────────────────────────────────┐    │
│  │        MySQL (数据持久化)            │    │
│  └──────────────────────────────────────┘    │
└─────────────────────────────────────────────┘
```

---

## 9. 数据库设计概要

### 9.1 表结构

```sql
-- 用户表
users (
    id          INT PRIMARY KEY AUTO_INCREMENT,
    username    VARCHAR(50) UNIQUE NOT NULL,
    password    VARCHAR(255) NOT NULL,  -- bcrypt hash
    avatar_url  VARCHAR(255),
    created_at  DATETIME DEFAULT NOW()
)

-- 笔记表
notes (
    id          INT PRIMARY KEY AUTO_INCREMENT,
    title       VARCHAR(255) NOT NULL DEFAULT '未命名笔记',
    content     LONGTEXT,               -- Markdown 原文
    owner_id    INT NOT NULL,
    created_at  DATETIME DEFAULT NOW(),
    updated_at  DATETIME DEFAULT NOW() ON UPDATE NOW(),
    FOREIGN KEY (owner_id) REFERENCES users(id)
)

-- 笔记协作者（可选：P1）
note_collaborators (
    id          INT PRIMARY KEY AUTO_INCREMENT,
    note_id     INT NOT NULL,
    user_id     INT NOT NULL,
    permission  ENUM('read', 'write') DEFAULT 'read',
    FOREIGN KEY (note_id) REFERENCES notes(id),
    FOREIGN KEY (user_id) REFERENCES users(id),
    UNIQUE(note_id, user_id)
)

-- 评论表（P1）
comments (
    id          INT PRIMARY KEY AUTO_INCREMENT,
    note_id     INT NOT NULL,
    user_id     INT NOT NULL,
    content     TEXT NOT NULL,
    created_at  DATETIME DEFAULT NOW(),
    FOREIGN KEY (note_id) REFERENCES notes(id),
    FOREIGN KEY (user_id) REFERENCES users(id)
)

-- 版本历史表（P2）
note_versions (
    id          INT PRIMARY KEY AUTO_INCREMENT,
    note_id     INT NOT NULL,
    content     LONGTEXT NOT NULL,
    version     INT NOT NULL,
    created_at  DATETIME DEFAULT NOW(),
    FOREIGN KEY (note_id) REFERENCES notes(id)
)
```

---

## 10. API 设计概要

### 10.1 REST API

| 方法 | 路径 | 说明 | 优先级 |
|------|------|------|--------|
| POST | `/api/auth/register` | 用户注册 | P0 |
| POST | `/api/auth/login` | 用户登录，返回 JWT | P0 |
| GET | `/api/notes` | 获取笔记列表 | P0 |
| POST | `/api/notes` | 创建笔记 | P0 |
| GET | `/api/notes/{id}` | 获取笔记详情 | P0 |
| PUT | `/api/notes/{id}` | 更新笔记元数据（标题等） | P0 |
| DELETE | `/api/notes/{id}` | 删除笔记 | P0 |
| POST | `/api/notes/{id}/share` | 分享笔记给用户 | P1 |
| GET | `/api/notes/{id}/comments` | 获取评论列表 | P1 |
| POST | `/api/notes/{id}/comments` | 发表评论 | P1 |
| POST | `/api/upload` | 上传文件 | P1 |

### 10.2 WebSocket

| 路径 | 说明 | 优先级 |
|------|------|--------|
| `ws://host/ws/notes/{id}` | 文档协作连接，负责 Yjs 同步消息转发 + 光标位置广播 | P0 |

**WebSocket 消息类型**：
- `sync` — Yjs 文档同步（CRDT 二进制数据）
- `awareness` — 用户在线状态/光标位置（用户名、光标行列、在线/离线）
- `chat`（P1）— 评论区实时消息

---

## 11. 约束条件

| 约束 | 说明 |
|------|------|
| **时间** | 3 天（AI 辅助开发） |
| **人力** | 1 人 |
| **数据库** | 必须使用 MySQL（开发者不熟悉 MongoDB） |
| **缓存** | 不使用 Redis（开发者不熟悉）—— WebSocket 状态由 FastAPI 内存管理 |
| **前端 UI** | 使用 PrimeVue |
| **部署** | 后端 Docker，前端 npm run dev，仅本地运行 |
| **文档格式** | 仅 Markdown，不做富文本 |
| **国际化** | 不需要 |

---

## 12. 风险与对策

| 风险 | 影响 | 对策 |
|------|------|------|
| **Yjs + FastAPI WebSocket 集成复杂** | 可能延误核心功能 | 优先用 y-websocket 标准协议对接，减少自定义代码 |
| **无 Redis，多进程部署会丢失 WebSocket 状态** | 仅本地单进程运行，不受影响 | 注意：Docker 中只启动 1 个 worker |
| **MySQL 存 Markdown 全文搜索效率低** | P2 功能，可接受 LIKE 搜索 | 暂不引入 ElasticSearch |
| **30分钟不做完所有 P1** | 范围可控 | P0 优先，P1 视情况裁减 |
| **实时协作 Yjs 学习成本** | Yjs 文档完善，AI 可辅助 | 核心集成代码量不大 |

---

## 13. 开发里程碑（建议）

| 天 | 目标 | 产出 |
|----|------|------|
| **Day 1** | 项目骨架 + 用户系统 + 基础笔记 CRUD | 前后端跑通，能注册登录、创建笔记 |
| **Day 2** | 实时协作核心（Yjs 集成 + WebSocket） | 两人可同时编辑，看到对方光标 |
| **Day 3** | UI 美化 + 评论 + 收尾 | 界面简洁大气，功能演示流畅 |

---

## 14. 未决问题

1. **PrimeVue 的哪个版本？** 建议 PrimeVue 4（最新），使用其 styled 模式或 unstyled + Tailwind 灵活定制。
2. **Markdown 编辑器的工具栏？** 是否需要加粗、标题等快捷按钮？建议 P0 简易工具栏（降低非技术人员门槛）。
3. **用户头像？** 建议用 DiceBear API 根据用户名生成默认头像，节省开发时间。
4. **Yjs 文档存储策略？** 建议每次用户断开连接时存入 MySQL，连接时从 MySQL 加载；或每 30 秒自动存一次。

---

## 15. 确认

> **请确认以上需求文档是否准确。**
> 
> 确认后我将授权架构师 Agent 进入**技术设计与编码阶段**。
> 
> 回复 **"确认" / "同意" / "Approved" / "开始设计"** 即可。

---

*本需求文档由产品经理 Agent 基于用户问答生成，版本 v1.0*
