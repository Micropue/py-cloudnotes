# 🐛 云笔记协作系统 Bug 分析报告

> **日期**: 2026-06-12 · **版本**: v1.1.0 · **严重程度**: 🔴 高

---

## 一、核心问题：实时协作不同步

### 现象
- 用户 A 编辑文档时，用户 B **看不到** A 的实时修改
- 同一文档退出再打开，内容不一致（有时看到旧版本）
- 同时编辑时，两边内容出现分歧

---

## 二、Bug 清单

### 🔴 Bug #1 (致命) — sync-response 覆盖实时 Yjs 状态

**文件**: `frontend/src/services/websocket.ts:218-230` + `backend/app/websocket/handler.py:122-128`

**时序问题**:

```
时间线:
  1. HTTP GET /notes/2 → 返回 DB 内容 "版本A"
  2. WebSocket 连接 → 收到用户 B 的实时 Yjs 增量 → Y.Doc 变为 "版本B"
  3. WebSocket 发送 sync-request
  4. 服务端返回 sync-response（DB 内容仍是 "版本A"，因为 B 的修改还没自动保存）
  5. sync-response 处理: delete(0, all) + insert(0, "版本A") → 把 Y.Doc 退化回 "版本A"
  6. 结果：用户 B 的实时修改被丢弃！
```

**根本原因**: `sync-response` 无条件 `delete(0, length) + insert(0, dbContent)`，覆盖了已通过 Yjs 增量同步到本地的实时内容。

**影响**: 多用户协作时，后打开文档的用户会覆盖先编辑用户的实时修改。

---

### 🔴 Bug #2 (致命) — WebSocket 连接无权限校验

**文件**: `backend/app/main.py:51-76`

```python
@app.websocket("/ws/notes/{note_id}")
async def websocket_note(websocket, note_id, token):
    payload = jose_jwt.decode(token, ...)
    user_id = payload.get("user_id")
    # ❌ 只验证了 JWT，没有调用 check_note_access()！
    await handle_websocket(websocket, note_id=note_id, ...)
```

**影响**: 
- 任何登录用户都可以连接任意笔记的 WebSocket
- 通过 `sync-request` 读取任意笔记内容
- 通过 `save` 消息覆盖任意笔记内容（见 Bug #3）

---

### 🔴 Bug #3 (致命) — WebSocket save 无写权限校验

**文件**: `backend/app/websocket/handler.py:130-132`

```python
elif msg_type == "save":
    await persist_document(note_id, data.get("content", ""))
    # ❌ 没有调用 check_note_access 验证写权限！
```

对比 HTTP API (`notes.py:72-76`)，那里正确调用了 `check_note_access` 并检查 `has_write`。

**影响**: 只读分享用户可以通过 WebSocket 绕过权限，直接覆盖文档内容。

---

### 🔴 Bug #4 (致命) — 共享文档权限泄露

**文件**: `backend/app/services/note_service.py:6-20`

```python
async def get_accessible_notes(db, user_id):
    share_exists = exists().where(
        ShareLink.note_id == Note.id,
        ShareLink.is_active == True,  # ❌ 任何活跃的分享链接都算
    )
    result = await db.execute(
        select(Note).where(or_(Note.owner_id == user_id, share_exists))
    )
```

**问题**: `share_exists` 只检查笔记是否**存在任何活跃分享链接**，而不是**当前用户是否被分享**。

**影响**:
- 用户 A 分享笔记给用户 B，但用户 C、D、E 也都能看到并编辑
- 没有 `note_collaborators` 表来跟踪具体哪些用户被授权

---

### 🟠 Bug #5 (严重) — 断线重连丢失未保存编辑

**文件**: `frontend/src/services/websocket.ts:296-301`

```typescript
private _resetConnection(): void {
    if (this.ydoc) {
        this.ydoc.destroy()   // ❌ 销毁 Y.Doc，丢失所有未持久化的本地编辑
        this.ydoc = null
    }
}
```

**影响**: WebSocket 断线重连时，Y.Doc 被销毁重建，`sync-request` 从数据库加载旧内容，用户未保存的编辑永久丢失。

---

### 🟠 Bug #6 (严重) — 退出时未自动保存

**文件**: `frontend/src/views/NoteEditorView.vue:340-345` + `backend/app/websocket/handler.py:143-148`

- 前端仅在 `onUnmounted` (正常路由导航) 时调用 `saveContent()`
- 浏览器直接关闭、崩溃时，最多丢失 **8 秒**的编辑内容
- 后端 `handle_websocket` 的 `finally` 块不保存内容

---

### 🟡 Bug #7 (中等) — 笔记列表无共享/自有区分

**文件**: `frontend/src/views/NotesListView.vue:47-77`

- 所有笔记卡片完全一样的样式
- 共享笔记和自有笔记无法区分
- 非所有者仍然看到"删除"按钮（虽然后端会返回 403）

---

### 🟡 Bug #8 (中等) — 服务端 rooms 字典并发隐患

**文件**: `backend/app/websocket/handler.py:83-84`

```python
if note_id in rooms and not rooms[note_id]:
    del rooms[note_id]
```

**TOCTOU 问题**: 在 `not rooms[note_id]` 检查和 `del rooms[note_id]` 之间，另一个协程可能添加新用户，导致误删非空房间。

---

## 三、Bug 优先级矩阵

```
                    影响大
                      │
       Bug#1 ─────────┼───────── Bug#2
       Bug#4          │          Bug#3
                      │
    ──────────────────┼──────────────────
                      │
       Bug#6          │          Bug#5
       Bug#7          │          Bug#8
                      │
                    影响小
```

---

## 四、修复建议（概要）

| Bug | 修复方向 | 涉及文件 |
|-----|---------|---------|
| #1 | sync-response 改为**合并**而非覆盖；或用 Yjs `encodeStateAsUpdate`  | `websocket.ts` |
| #2 | WebSocket 端点调用 `check_note_access()` | `main.py` |
| #3 | save 消息处理前调用 `check_note_access()` | `handler.py` |
| #4 | 添加 `note_collaborators` 表，分享时绑定用户 | `models.py`, `share.py`, `note_service.py` |
| #5 | 重连时保留旧的 Y.Doc，或先 `encodeStateAsUpdate` 保存 | `websocket.ts` |
| #6 | `beforeunload` 事件中保存；`disconnect` 时后端自动保存 | `NoteEditorView.vue`, `handler.py` |
| #7 | 笔记卡片添加标签（"共享"/"我的"）；隐藏非所有者的删除按钮 | `NotesListView.vue` |
| #8 | 加 `asyncio.Lock` 保护 rooms 操作 | `handler.py` |

---

## 五、测试验证清单

- [ ] 用户 A 编辑时，用户 B 能否实时看到（不退出重进）
- [ ] 用户 A 和 B 同时编辑，内容是否正确合并（不是最后写入者胜）
- [ ] 用户 C（未分享）能否访问被分享的文档
- [ ] 断网后重连，未保存内容是否保留
- [ ] 只读分享用户能否通过 WebSocket 写入
- [ ] 笔记列表是否区分共享/自有
- [ ] 非所有者是否无法删除他人笔记

---

*报告由 AI 辅助分析生成 · 请人工复核*
