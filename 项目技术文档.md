# ☁️ 云笔记系统 — 项目技术文档

> **版本**: v1.1.0  
> **最后更新**: 2026-06-12  
> **项目类型**: 个人学习项目 / 实时协作 Markdown 云笔记  

---

## 目录

1. [项目概述](#1-项目概述)
2. [技术栈](#2-技术栈)
3. [系统架构](#3-系统架构)
4. [项目结构](#4-项目结构)
5. [数据库设计](#5-数据库设计)
6. [API 接口文档](#6-api-接口文档)
7. [WebSocket 协议](#7-websocket-协议)
8. [前端路由](#8-前端路由)
9. [实时协作机制](#9-实时协作机制)
10. [部署与运行](#10-部署与运行)
11. [配置说明](#11-配置说明)
12. [Docker 管理](#12-docker-管理)

---

## 1. 项目概述

### 1.1 项目定位

云笔记是一个基于 **Markdown** 的实时协作笔记系统，支持多人同时编辑同一篇文档，实时同步内容与光标位置。

### 1.2 核心功能

| 功能 | 说明 |
|------|------|
| 用户系统 | 注册 / 登录（JWT 鉴权，24小时有效期） |
| 笔记管理 | 创建、编辑、删除 Markdown 笔记 |
| 分屏预览 | 左编辑 / 右实时预览，滚动同步 |
| 实时协作 | 多人同时编辑，Yjs CRDT 冲突合并 |
| 光标同步 | 协作者光标实时显示（Canvas 精确坐标） |
| 在线状态 | 协作者头像列表 + 绿色在线圆点 |
| 分享链接 | 生成/复制/撤销分享链接（可读写权限） |
| 评论系统 | 文档底部评论，Enter 快捷发送 |
| 文件上传 | 图片上传，Markdown 引用（≤4MB） |
| Markdown 工具栏 | 加粗/斜体/标题/列表/代码/链接/图片 |

---

## 2. 技术栈

### 2.1 前端

| 技术 | 版本 | 用途 |
|------|------|------|
| Vue 3 | ^3.5.13 | 前端框架 (Composition API) |
| TypeScript | ~5.6.3 | 类型安全 |
| Vite | ^6.0.5 | 构建工具 / 开发服务器 |
| PrimeVue 4 | ^4.2.5 | UI 组件库 (Aura 主题) |
| PrimeIcons | ^7.0.0 | 图标库 |
| Pinia | ^2.2.6 | 状态管理 |
| Vue Router | ^4.4.5 | 路由管理 |
| Axios | ^1.7.9 | HTTP 客户端 |
| Yjs | ^13.6.20 | CRDT 实时协作算法 |
| markdown-it | ^14.1.0 | Markdown 渲染 |

### 2.2 后端

| 技术 | 版本 | 用途 |
|------|------|------|
| Python | 3.11 | 运行环境 |
| FastAPI | 0.115.0 | Web 框架 |
| Uvicorn | 0.30.6 | ASGI 服务器 |
| SQLAlchemy | 2.0.35 | ORM（异步模式） |
| asyncmy | 0.2.9 | MySQL 异步驱动 |
| python-jose | 3.3.0 | JWT 签发与验证 |
| passlib | 1.7.4 | 密码哈希 (bcrypt) |
| websockets | 12.0 | WebSocket 支持 |
| greenlet | 3.0.3 | 异步协程依赖 |

### 2.3 基础设施

| 技术 | 版本 | 用途 |
|------|------|------|
| MySQL | 8.0 | 关系数据库 |
| Docker | 29.x | 容器化部署 |
| Docker Compose | v3 | 多容器编排 |

---

## 3. 系统架构

```
┌─────────────────────────────────────────────────────────┐
│                    浏览器 (Vue 3)                        │
│  ┌──────────┐ ┌──────────┐ ┌─────────────────────────┐  │
│  │ 登录/注册 │ │ 笔记列表  │ │    编辑器 (分屏)         │  │
│  │          │ │          │ │  ┌─────────┬───────────┐ │  │
│  │          │ │          │ │  │ 编辑区   │ 预览区     │ │  │
│  │          │ │          │ │  │(textarea)│(markdown) │ │  │
│  │          │ │          │ │  │ Yjs CRDT │  渲染      │ │  │
│  │          │ │          │ │  └─────────┴───────────┘ │  │
│  │          │ │          │ │  工具栏 │ 协作者 │ 评论    │  │
│  └────┬─────┘ └────┬─────┘ └──────────┬──────────────┘  │
│       │            │                  │                  │
│       │   HTTP REST (Axios)          │ WebSocket (Yjs)  │
└───────┼────────────┼──────────────────┼──────────────────┘
        │            │                  │
   ┌────▼────────────▼──────────────────▼──────────────────┐
   │              Nginx / Vite Proxy (:5173)                │
   │    /api → :8004     /ws → :8004    /uploads → :8004   │
   └───────────────────────┬───────────────────────────────┘
                           │
   ┌───────────────────────▼───────────────────────────────┐
   │           Docker: cloudnotes-backend (:8004)           │
   │  ┌──────────┐ ┌──────────┐ ┌────────────────────────┐ │
   │  │ Auth API │ │ Note API │ │  WebSocket Handler     │ │
   │  │ /auth/*  │ │ /notes/* │ │  /ws/notes/{id}        │ │
   │  ├──────────┤ ├──────────┤ │  • Yjs 消息广播        │ │
   │  │ Share    │ │ Comment  │ │  • 光标位置中继        │ │
   │  │ /share/* │ │ /notes/  │ │  • 在线状态管理        │ │
   │  │          │ │ {id}/    │ │  • 文档持久化          │ │
   │  │          │ │ comments │ │                        │ │
   │  └──────────┘ └──────────┘ └───────────┬────────────┘ │
   │                                        │               │
   └────────────────────────────────────────┼───────────────┘
                                            │
   ┌────────────────────────────────────────▼───────────────┐
   │           Docker: cloudnotes-mysql (:3309)              │
   │  ┌────────────────────────────────────────────────────┐ │
   │  │  数据库: cloudnotes                                │ │
   │  │  ├── users           (用户)                        │ │
   │  │  ├── notes           (笔记)                        │ │
   │  │  ├── share_links     (分享链接)                    │ │
   │  │  └── comments        (评论)                        │ │
   │  └────────────────────────────────────────────────────┘ │
   └────────────────────────────────────────────────────────┘
```

---

## 4. 项目结构

```
云笔记/
├── 项目技术文档.md              ← 本文档
├── 需求文档.md                  ← 需求规格说明书
│
├── backend/                     ← 后端 (Python FastAPI)
│   ├── .env                     ← Docker Compose 项目名称
│   ├── Dockerfile               ← Python 3.11-slim 镜像
│   ├── docker-compose.yml       ← MySQL + FastAPI 编排
│   ├── requirements.txt         ← Python 依赖
│   ├── uploads/                 ← 上传文件目录
│   └── app/
│       ├── main.py              ← 应用入口 + 路由注册 + lifespan
│       ├── config.py            ← 数据库/JWT/上传目录配置
│       ├── database.py          ← 异步数据库连接引擎
│       ├── models.py            ← ORM 模型 (4 张表)
│       ├── schemas.py           ← Pydantic 请求/响应模型
│       ├── routers/
│       │   ├── auth.py          ← 注册 / 登录 / 获取用户信息
│       │   ├── notes.py         ← 笔记 CRUD
│       │   ├── share.py         ← 分享链接 + 协作者列表
│       │   ├── comments.py      ← 评论 CRUD
│       │   └── upload.py        ← 文件上传
│       ├── services/
│       │   ├── auth_service.py  ← 密码加密 / JWT 签发验证
│       │   └── note_service.py  ← 权限检查 / 笔记查询
│       └── websocket/
│           └── handler.py       ← Yjs 中继 / 光标广播 / 在线状态
│
├── frontend/                    ← 前端 (Vue 3 + TypeScript)
│   ├── package.json             ← npm 依赖
│   ├── vite.config.ts           ← Vite 配置 + API 代理
│   ├── tsconfig.json            ← TypeScript 配置
│   ├── index.html               ← 入口 HTML
│   └── src/
│       ├── main.ts              ← Vue 挂载 + PrimeVue/Tooltip 注册
│       ├── App.vue              ← 根组件 + 全局样式
│       ├── router/index.ts      ← 路由表 + 鉴权守卫
│       ├── stores/auth.ts       ← Pinia 认证状态
│       ├── services/
│       │   ├── api.ts           ← Axios 封装 (JWT 拦截器)
│       │   └── websocket.ts     ← Yjs WebSocket 客户端
│       ├── views/
│       │   ├── LoginView.vue    ← 登录页 (左右分栏)
│       │   ├── RegisterView.vue ← 注册页
│       │   ├── NotesListView.vue← 笔记列表 (平铺卡片)
│       │   ├── NoteEditorView.vue← 核心编辑器 (分屏+协作)
│       │   └── JoinView.vue     ← 分享链接加入页
│       └── components/
│           ├── CommentSection.vue  ← 评论区
│           └── ShareLinkDialog.vue ← 分享链接对话框
```

---

## 5. 数据库设计

### 5.1 数据库信息

| 配置项 | 值 |
|--------|-----|
| 数据库类型 | MySQL 8.0 |
| 数据库名 | `cloudnotes` |
| 用户名 | `cloudnotes` |
| 密码 | `cloudnotes123` |
| Root 密码 | `root123456` |
| 字符集 | utf8mb4 (默认) |
| 宿主机端口 | `3309` |
| 容器内端口 | `3306` |

### 5.2 表结构

#### users — 用户表

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | INT | PK, AUTO_INCREMENT | 用户ID |
| username | VARCHAR(50) | UNIQUE, NOT NULL, INDEX | 用户名 |
| password | VARCHAR(255) | NOT NULL | bcrypt 哈希 |
| avatar_url | VARCHAR(255) | DEFAULT '' | DiceBear 头像 |
| created_at | DATETIME | DEFAULT NOW() | 注册时间 |

#### notes — 笔记表

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | INT | PK, AUTO_INCREMENT | 笔记ID |
| title | VARCHAR(255) | NOT NULL, DEFAULT '未命名笔记' | 标题 |
| content | TEXT | DEFAULT '' | Markdown 内容 |
| owner_id | INT | FK → users.id, NOT NULL | 所有者 |
| created_at | DATETIME | DEFAULT NOW() | 创建时间 |
| updated_at | DATETIME | ON UPDATE NOW() | 更新时间 |

#### share_links — 分享链接表

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | INT | PK, AUTO_INCREMENT | 链接ID |
| note_id | INT | FK → notes.id, NOT NULL | 笔记ID |
| token | VARCHAR(64) | UNIQUE, NOT NULL, INDEX | 唯一分享令牌 |
| permission | ENUM('read','write') | DEFAULT 'read' | 权限 |
| is_active | BOOLEAN | DEFAULT TRUE | 是否有效 |
| created_by | INT | FK → users.id, NOT NULL | 创建者 |
| created_at | DATETIME | DEFAULT NOW() | 创建时间 |

#### comments — 评论表

| 字段 | 类型 | 约束 | 说明 |
|------|------|------|------|
| id | INT | PK, AUTO_INCREMENT | 评论ID |
| note_id | INT | FK → notes.id, NOT NULL | 笔记ID |
| user_id | INT | FK → users.id, NOT NULL | 评论者 |
| content | TEXT | NOT NULL | 评论内容 |
| created_at | DATETIME | DEFAULT NOW() | 创建时间 |

### 5.3 ER 关系图

```
users (1) ────< (N) notes
  │                 │
  │ (1)             │ (1)
  │                 │
  ├──< (N) comments >──┤
  │                 │
  └──< (N) share_links >──┘
```

---

## 6. API 接口文档

### 6.1 基础信息

| 项目 | 值 |
|------|-----|
| Base URL | `http://localhost:8004` |
| 鉴权方式 | `Authorization: Bearer <JWT>` |
| Content-Type | `application/json` |

### 6.2 健康检查

| 方法 | 路径 | 鉴权 | 说明 |
|------|------|------|------|
| GET | `/api/health` | 无 | 服务健康检查 |

### 6.3 用户认证

| 方法 | 路径 | 鉴权 | 请求体 | 响应 | 说明 |
|------|------|------|--------|------|------|
| POST | `/api/auth/register` | 无 | `{username, password}` | `{access_token, user}` | 注册 |
| POST | `/api/auth/login` | 无 | `{username, password}` | `{access_token, user}` | 登录 |
| GET | `/api/auth/me` | Bearer | — | `{id, username, avatar_url}` | 获取当前用户 |

### 6.4 笔记管理

| 方法 | 路径 | 鉴权 | 说明 |
|------|------|------|------|
| GET | `/api/notes` | Bearer | 获取笔记列表（自己的 + 分享链接公开的） |
| POST | `/api/notes` | Bearer | 创建笔记 `{title?, content?}` |
| GET | `/api/notes/{id}` | Bearer | 获取笔记详情 |
| PUT | `/api/notes/{id}` | Bearer | 更新笔记 `{title?, content?}` |
| DELETE | `/api/notes/{id}` | Bearer | 删除笔记（仅所有者） |

### 6.5 分享链接

| 方法 | 路径 | 鉴权 | 说明 |
|------|------|------|------|
| POST | `/api/notes/{id}/share-link` | Bearer | 创建分享链接 `{permission}` |
| GET | `/api/notes/{id}/share-link` | Bearer | 查询活跃分享链接 |
| DELETE | `/api/notes/{id}/share-link` | Bearer | 停止分享（撤销链接） |
| GET | `/api/share/{token}` | Bearer | 通过分享链接加入 |
| GET | `/api/notes/{id}/collaborators` | Bearer | 获取协作者列表 |

### 6.6 评论

| 方法 | 路径 | 鉴权 | 说明 |
|------|------|------|------|
| GET | `/api/notes/{id}/comments` | Bearer | 获取评论列表 |
| POST | `/api/notes/{id}/comments` | Bearer | 发表评论 `{content}` |

### 6.7 文件上传

| 方法 | 路径 | 鉴权 | 说明 |
|------|------|------|------|
| POST | `/api/upload` | Bearer | 上传图片 (multipart, ≤4MB) |

**上传响应**:
```json
{ "url": "/uploads/abc123.jpg", "filename": "abc123.jpg" }
```

---

## 7. WebSocket 协议

### 7.1 连接

```
ws://localhost:8004/ws/notes/{note_id}?token=<JWT>
```

### 7.2 消息类型

| 方向 | 类型 | 格式 | 说明 |
|------|------|------|------|
| 客户端→服务端 | 二进制 | Yjs Update (Uint8Array) | CRDT 文档增量 |
| 服务端→客户端 | 二进制 | Yjs Update (Uint8Array) | 广播给其他客户端 |
| 客户端→服务端 | JSON | `{"type":"cursor","cursor":{"line":N,"col":N}}` | 光标位置 |
| 客户端→服务端 | JSON | `{"type":"sync-request"}` | 请求加载文档 |
| 服务端→客户端 | JSON | `{"type":"sync-response","content":"..."}` | 返回文档内容 |
| 客户端→服务端 | JSON | `{"type":"save","content":"..."}` | 手动保存 |
| 服务端→客户端 | JSON | `{"type":"awareness","users":[...]}` | 在线用户 + 光标 |

### 7.3 Awareness 消息结构

```json
{
  "type": "awareness",
  "users": [
    {
      "user_id": 1,
      "username": "alice",
      "avatar_url": "https://api.dicebear.com/9.x/initials/svg?seed=alice",
      "cursor": { "line": 5, "col": 12 }
    }
  ]
}
```

---

## 8. 前端路由

| 路径 | 页面 | 鉴权 | 说明 |
|------|------|------|------|
| `/login` | LoginView | 游客 | 登录页（支持 `?redirect=` 参数） |
| `/register` | RegisterView | 游客 | 注册页 |
| `/` | NotesListView | 需登录 | 笔记列表（平铺卡片） |
| `/note/:id` | NoteEditorView | 需登录 | 编辑器（分屏+实时协作） |
| `/join/:token` | JoinView | 需登录 | 通过分享链接加入 |

**路由守卫逻辑**:
- 需登录页面 → 未登录 → 跳转 `/login?redirect=<原路径>`
- 游客页面（登录/注册）→ 已登录 → 跳转 `/`

---

## 9. 实时协作机制

### 9.1 架构

```
用户 A 编辑                    用户 B 编辑
    │                              │
    ▼ (Yjs CRDT)                   ▼ (Yjs CRDT)
┌───────────┐                 ┌───────────┐
│ Y.Doc     │                 │ Y.Doc     │
│ Y.Text    │                 │ Y.Text    │
└─────┬─────┘                 └─────┬─────┘
      │ WebSocket                   │ WebSocket
      │ (二进制 Yjs Update)          │ (二进制 Yjs Update)
      ▼                             ▼
┌─────────────────────────────────────────┐
│       FastAPI WebSocket 中继             │
│                                         │
│  rooms[note_id] = {                     │
│    ws_A: {user_id, username, cursor},   │
│    ws_B: {user_id, username, cursor},   │
│  }                                      │
│                                         │
│  • 收到二进制消息 → 广播给其他客户端      │
│  • 收到 cursor → 收集 → 广播 awareness  │
│  • 收到 save → 异步写入 MySQL           │
│  • 收到 sync-request → 从 MySQL 加载    │
└─────────────────────────────────────────┘
```

### 9.2 Yjs 同步策略

| 方向 | 机制 |
|------|------|
| **本地→Yjs** | 每次 `textarea` input 事件触发 `syncLocalChange()`，全量覆盖 Y.Text |
| **Yjs→服务器** | `ydoc.on('update')` → 发送二进制增量到 WebSocket |
| **服务器→远程** | 广播给房间内除发送者外的所有客户端 |
| **远程→本地** | `Y.applyUpdate()` 应用到本地 Y.Doc |
| **本地回显保护** | `_isLocalChange` 标志阻止本地修改回显 |

### 9.3 光标同步

- 发送频率：每 **200ms** 采集一次
- 坐标计算：使用 **Canvas 2D measureText()** 精确计算每个字符像素位置
- 中文字符自动按 2 倍宽度处理
- 自动扣除 textarea scrollTop/padding 偏移

---

## 10. 部署与运行

### 10.1 环境要求

| 依赖 | 最低版本 |
|------|----------|
| Docker | 20.x+ |
| Docker Compose | v2+ |
| Node.js | 18.x+ |
| npm | 9.x+ |

### 10.2 启动步骤

```bash
# 1. 克隆项目
cd 云笔记

# 2. 启动后端（MySQL + FastAPI）
cd backend
docker-compose up -d --build

# 3. 等待服务就绪（约 30 秒）
docker-compose ps
# 预期输出：cloudnotes-mysql (healthy), cloudnotes-backend (running)

# 4. 启动前端
cd ../frontend
npm install        # 仅首次
npm run dev

# 5. 浏览器打开
open http://localhost:5173
```

### 10.3 端口映射

| 服务 | 宿主机 | 容器内 | 说明 |
|------|--------|--------|------|
| 前端 (Vite) | `5173` | — | 本地开发服务器 |
| 后端 (FastAPI) | `8004` | `8000` | REST API + WebSocket |
| MySQL | `3309` | `3306` | 数据库 |

### 10.4 停止服务

```bash
# 停止后端
cd backend && docker-compose down

# 停止前端 (Ctrl+C)
```

---

## 11. 配置说明

### 11.1 后端环境变量

| 变量 | 默认值 | 说明 |
|------|--------|------|
| `DATABASE_URL` | `mysql+asyncmy://cloudnotes:cloudnotes123@mysql:3306/cloudnotes` | 数据库连接 |
| `SECRET_KEY` | `cloud-notes-secret-key-...` | JWT 签名密钥 |
| `UPLOAD_DIR` | `/app/uploads` | 文件上传目录 |

### 11.2 JWT 配置

| 参数 | 值 |
|------|-----|
| 算法 | HS256 |
| 有效期 | 24 小时 |
| 载荷 | `{user_id, username, exp}` |

### 11.3 上传限制

| 参数 | 值 |
|------|-----|
| 最大文件大小 | 4 MB |
| 允许格式 | .jpg, .jpeg, .png, .gif, .webp, .svg, .bmp |
| 存储位置 | `/app/uploads/` (容器内) |

### 11.4 前端 Vite 代理

```typescript
// vite.config.ts
proxy: {
  '/api':     'http://localhost:8004',  // REST API
  '/ws':      'ws://localhost:8004',    // WebSocket
  '/uploads': 'http://localhost:8004',  // 静态文件
}
```

---

## 12. Docker 管理

### 12.1 常用命令

```bash
cd backend

# 构建并启动
docker-compose up -d --build

# 查看状态
docker-compose ps

# 查看日志
docker-compose logs -f backend    # 后端日志
docker-compose logs -f mysql      # 数据库日志

# 进入容器
docker exec -it cloudnotes-backend bash
docker exec -it cloudnotes-mysql mysql -u cloudnotes -pcloudnotes123 cloudnotes

# 重启
docker-compose restart backend

# 停止并清理
docker-compose down               # 停止，保留数据卷
docker-compose down -v            # 停止，删除数据卷（清空数据库）
```

### 12.2 容器信息

| 容器名 | 镜像 | 端口 | 数据卷 |
|--------|------|------|--------|
| `cloudnotes-mysql` | `mysql:8.0` | `3309:3306` | `mysql_data` |
| `cloudnotes-backend` | `cloudnotes-backend` (构建) | `8004:8000` | `./uploads` |

---

> **文档维护者**: AI Agent  
> **生成日期**: 2026-06-12
